-- current stamper version
-- written by primiti-ve on github

local currentVersion = {
	major = `0`,
	minor = `1`,
	patch = `0`,
}

local baseUrl = `https://stamper-registry.primiti-ve.workers.dev`

return {
	currentVersion = currentVersion,
	baseUrl = baseUrl,
}
